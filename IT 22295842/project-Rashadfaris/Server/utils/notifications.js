export const sendEmailNotification = (userEmail, subject, message) => {
    // Placeholder for email sending logic
    // You can integrate with services like Nodemailer or any third-party email provider
    console.log(`Sending email to ${userEmail} with subject: ${subject}`);
    console.log(`Message: ${message}`);
  };
  